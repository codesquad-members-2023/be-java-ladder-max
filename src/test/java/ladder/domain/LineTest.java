package ladder.domain;

import org.junit.jupiter.api.*;

import static org.assertj.core.api.Assertions.*;

public class LineTest {
    @Test
    @DisplayName("같은 높이에 연속된 발판이 없는지 검증하는 테스트")
    void checkContinuousRungs() {
        // given

        // when

        // then
    }
}
